main: Η συνάρτηση main αναλαμβάνει το άνοιγμα και κλείσιμο του server
Δημιουργεί ένα pipe για επικοινωνία στο οποίο δίνει δικαιώματα γράφης ,ανάγνωσης και εκτέλεσης
και ένα server file, το οποίο θα περιέχει το pid του server

jobExecutorServer:Το αρχείο αυτό υλοποιεί τον jobExecutorServer
Διαβάζει αρχίκα την εντολή από τον χρηστή και διακρινεί τον τύπο της εντολής (issueJob,setConcurrency,stop,poll,exit)
Ανάλογα με τον τύπο της εντολής εκτέλει και την κατάλληλη λειτουργία, στέλνοντας πάντα στον jobCommander κατάλληλα μηνύματα.

Αν η εντολή είναι η issueJob τότε υλοποίω μια συνάρτηση void issueJob(char *command,ActiveList *activelist,QueuedList *queuedlist,int jobID,unsigned short int inst),
η οποία υλοποιεί την εντολή.Η συνάρτηση στέλνει αρχίκα ένα σήμα SIGCHLD στην δίεργασία γόνεα όταν η διεργασία παιδί έχει αλλάξει κατάσταση (έδω γίνεται active και απόκτα pid),
το σήμα αυτό το διαχειρίζεται ο handler. O handler είναι μια άλλη συνάρτηση που υλοποίω.Αρχίκα υπάρχει ένα while το οποίο περίμενει όλες τις διεργασίες παιδία να τερματιστούν και μέτα την αφαιρεί από την ούρα των active list.
Επομένως αν έχει δημιουργηθεί χώρος στην λίστα με τις active από την αφαίρεση των διεργασίων παιδία,τότε ο handler προσθέτει διεργασίες που βρίσκονται σε αναμονή(queued list) στην active list.
Επιστρέφουμε στην issueJob η οποία διακρίνει τις εξής περιπτώσεις, ανάλογα με το όρισμα inst, το οποίο λείτουργει σαν flag 0 ή 1.
Οι περιπτώσεις είναι οι εξής: αν το inst είναι 1, τότε σημαίνει οτί μια active διεργασία τελείωσε και μια queued θα γίνει active.
Άρα έχουμε τις περιπτώσεις μια queued διεργασία να γίνει active, η νέα διεργασία να γίνει κατευθείαν active και η νέα διεργασία να γίνει queued επειδή δεν υπάρχει χώρος στην active list.

Αν η εντόλη είναι setConcurrency,τότε άλλαζει το concurrency και κάλει την συνάρτηση void update(), η οποία προσαρμόζει τo μέγεθος της λίστας των active με το νέο concurrency.

Αν η εντόλη είναι stop, τότε ψάχνει την διεργασία με το συγκεκριμένο id και στην λίστα των active και στην λίστα των queued
Αν βρέθει, τότε σταματαεί την συγκεκρίμενη διεργασία, αν όχι στέλνει κατάλληλο μήνυμα στον jobCommander.

Αν η εντολή είναι poll, ανάλογα με το όρισμα running, queued επιστρέφει για όλεσ τις διεργασίεσ active και queued αντιστοιχα την τριπλετα <jobID,job,queuePosition>

Αν η εντολή είναι exit, κάνει unlink τον server και στέλνει μηνύμα εξόδου στον jobCommander.


jobCommander:Η συνάρτηση αυτή διαχειρίζεται τον jobCommander και τα pipes για την ανάγνωση μηνύματων από τον jobExecutorServer

communication:Το αρχείο αυτό περιέχει συναρτήσεις για άνοιγμα pipes για επικοινωνία
Η void openNamePipeForReading() ανοίγει ένα pipe μόνο για δίαβασμα μηνύματων
Η void openNamePipeForWritingAndRespond(char *) είναι η βασίκη συνάρτηση για την επικοινωνία και αποστολή μηνύματων που χρησιμοποιώ στην υλοποίηση
Η void openNamePipeForWritingAndRespondTheTriplet(char **) είναι η ίδια συνάρτηση με την παραπάνω, απλά αντί να επιστρέφει ένα string επιστρέφει έναν πίνακα
Ο πίνακας που επίστρεφει αντιστοιχεί στην τριπλέτα  <jobID,job,queuePosition> που ζητείτε στην εκφώνηση.



list:Το αρχείο αυτό περίεχει συνάρτησεις για την διαχείρηση των δύο λίστων που χρησιμοποιούνται στην υλοποίηση.
Έχουμε την λίστα με τις διεργασίες που εκτελούνται τώρα, Active List
και έχουμε και την λίστα με τις διεργασίες που έχουν μπεί σε ούρα αναμονής και περιμένουν να εκτελεστούν 
όταν υπάρξει διαθέσιμος χώρος στην active list, η λίστα αυτή ονομάζεται queued list.


multijob:λαμβάνει σαν είσοδο αρχεία τα οποία
περιέχουν εργασίες προς εκτέλεση και τις εισάγει στο σύστημα


allJobsStop:Το script αυτό σταματά όλες τις εργασίες που υπάρχουν ενεργές στον
jobExecutor.

Εκτέλεση:
Για την εκτέλεση του προγράμματος: 
Με την σείρα οι εντολές
```bash 
make clean
```
```bash
make
```
και στην συνέχεια δίνεται στο terminal οποίαδηπωτε εντολή της μορφής 
```bash
./jobCommander 
```

Για την εκτέλεση των scripts μπορεί να χρειαστεί να δωθεί permsission, αν χρείαστει τότε να δώθει η εντολή 
```bash
chmod + x multijob.sh
```

```bash
chmod + x allJobsStop.sh
```

Σε αρχεία έχουν προσθέθει εικόνα από ενδεικτική εκτέλεση (endiktiki ektelesi 1.png,endiktiki ektelesi 2.png)


